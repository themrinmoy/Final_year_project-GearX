<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #cc5522;">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="img/logomrin.png" height="40px" alt="Logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="fw-bold nav-link <%= path === '/' ? 'active': '' %>" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="fw-bold nav-link <%= path === '/rent' ? 'active': '' %>" href="/rent/">Rent</a>
                </li>
                <li class="nav-item">
                    <a class="fw-bold nav-link <%= path === '/products' ? 'active': '' %>" href="/products">Products</a>
                </li>
                <% if(isAuthenticated){ %>
                    <% if (userType==='admin' ) { %>
                        <li class="nav-item">
                            <a class="fw-bold nav-link <%= path === '/admin' ? 'active': '' %>" href="/admin">Admin</a>
                        </li>
                    <% } %>
                <% } %>
                <% if(isAuthenticated){ %>
                    <li class="nav-item">
                        <a class="fw-bold nav-link <%= path === '/order' ? 'active': '' %>" href="/order">Order</a>
                    </li>
                    <li class="nav-item">
                        <a class="fw-bold nav-link <%= path === '/user/rentals' ? 'active': '' %>" href="/user/rentals">Rentals</a>
                    </li>
                    <li class="nav-item">
                        <a class="fw-bold nav-link <%= path === '/rent/cart' ? 'active': '' %>" href="/rent/cart">Rent cart</a>
                    </li>
                <% } %>
            </ul>
        </div>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link <%= path === '/favorites' ? 'active': '' %>" href="/favorites"><i class="fas fa-heart"></i></a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= path === '/cart' ? 'active': '' %>" href="/cart"><i class="fas fa-shopping-cart"></i></a>
            </li>
            <% if(isAuthenticated){ %>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="/logout">Logout</a>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="/login">Log In</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="/signup">Sign Up</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>

<script>
    const navbarToggler = document.querySelector(".navbar-toggler");
    const navbarCollapse = document.querySelector(".navbar-collapse");

    navbarToggler.addEventListener('click', () => {
        navbarCollapse.classList.toggle('show');
    });
</script>
